We are fixing a constructor-order bug in MetadataVisualizer.

Current problem:
- WindowVisualizer.__init__() immediately calls self.create_browser().
- MetadataVisualizer.create_browser() uses self._project_path.
- That means MetadataVisualizer must initialize _project_path before calling super().__init__().

Task:
1. Fix MetadataVisualizer so _project_path is assigned before super().__init__(pref_key="window_metadata").
2. Audit MetadataVisualizer.__init__() for any other attributes used by create_browser() or create_inspector() and initialize those before super().__init__() as well.
3. Do not change WindowVisualizer.
4. Do not add new abstractions.
5. Do not refactor the inspector or browser architecture.
6. Keep the visual/UI behavior identical.

Expected minimal fix:
- Move self._project_path = project_path above super().__init__(pref_key="window_metadata").

Validation:
- crossing visualizer metadata should open without AttributeError.
- Clicking Metadata from the Project visualizer should also open cleanly.
- No other behavior should change.