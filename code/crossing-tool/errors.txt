I'll mark the import-check todo as completed.

Completed: *Run import checks and report stylesheet/widget change* (4/5)

Yes — the patches were applied. Quick summary and exact locations:

- **TabPanel:** tab_panel.py — inspector now uses the standard `JumpScrollBar()` and `setFocusPolicy(Qt.NoFocus)`; I removed the synthetic viewport event-forwarding so behaviour matches the browser.
- **Corner widget hidden:** tab_panel.py — `corner.setFixedSize(0, 0)` and `corner.hide()` removed the small grey box.
- **Metadata table/perimeter removed:** metadata_block.py — removed the `gridline-color` entry; metadata_block.py — `self._table.setShowGrid(False)` (no native grid) and `self._table.setVerticalScrollBar(JumpScrollBar())`.
- **Import check (what I ran):**
```bash
python3 - << 'PY'
import sys
sys.path.insert(0,'/home/cowpoke/playable/head-irad-playable-cinema/code/crossing-tool')
import importlib
from styles import theme
print('SCROLLBAR_W=',theme.SCROLLBAR_W,'TAB_BG=',theme.TAB_BG)
import visualizers.metadata_visualizer as mv
importlib.reload(mv)
print('reload ok')
PY
```
Output: `SCROLLBAR_W= 8 TAB_BG= #00FF00` and `reload ok`.

If you still don't see changes in your editor/UI: try re-opening the files or restart the app (or run the import command above). Want me to revert the debug `theme.TAB_BG` green now?