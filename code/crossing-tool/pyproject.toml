[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "crossing"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "av",
    "sounddevice",
    "paddlepaddle>=2.6",
    "paddleocr>=2.9",
    "numpy",
    "Pillow",
]

[project.scripts]
crossing = "cli:main"

[tool.setuptools]
py-modules = ["cli", "prefs"]

[tool.setuptools.packages.find]
where = ["."]
